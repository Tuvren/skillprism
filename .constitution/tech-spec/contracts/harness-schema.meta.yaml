artifact: harness-schema.json
surface: json
flows: [flow-builtin-harness, flow-override-harness]
boundaries: [BND-03]
certainty: assumed
assumption: "JSON Schema for a harness definition. Moving it into the code is the upgrade's artifact phase."
settles_when: "A test validates a built-in harness definition against this schema."
