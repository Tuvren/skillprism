artifact_path: schemas/skill-schema.json
surface: json
flows: [flow-compile-template, flow-merge-variables, flow-copy-assets]
boundaries: [BND-02]
certainty: assumed
assumption: "The schema was aligned with SkillYamlRaw after the move. It describes the supported shape, not every value the parser silently ignores."
settles_when: "A test validates a skill file against this schema."
