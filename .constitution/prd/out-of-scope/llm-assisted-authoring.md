# Out of Scope: LLM-Assisted Template Authoring

**Context:** Determined out of scope during initial scope definition.

**Reasoning:** An AI-assisted template editor or generator is a separate product. skillprism is a deterministic build tool. Template authoring happens in any text editor; the tool's role is compilation and validation only.
