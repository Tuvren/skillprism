# Out of Scope: Variable Schema Enforcement

**Context:** Determined non-goal during initial scope definition.

**Reasoning:** skillprism resolves the variables that skills define but does not prescribe or enforce a schema for what those variables must be. Schema enforcement would require a variable type system and validation rules that are outside the tool's responsibility. Users define their own variable contracts.
