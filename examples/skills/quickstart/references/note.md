# Just a real file

This file lives in `quickstart/references/` and is copied byte-for-byte into every
harness's output directory alongside the rendered `SKILL.md`. skillprism doesn't care
what this directory is named — see `mcp-builder/reference/` (singular) and
`webapp-testing/examples/` elsewhere in this repo for two more real names that work
exactly the same way.
